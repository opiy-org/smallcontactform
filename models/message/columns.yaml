# ===================================
#  Column Definitions
# ===================================

columns:

    # id:
    #     label: opiy.smallgallery::lang.categories.columns.id
    #     searchable: true

    new_message:
        label: opiy.faq::lang.models.message.columns.new_message
        type: switch_icon_star

    created_at:
        label: opiy.faq::lang.models.message.columns.datetime
        type: datetime
        searchable: true

    name:
        label: opiy.faq::lang.models.message.columns.name
        searchable: true

    email:
        label: opiy.faq::lang.models.message.columns.email
        searchable: true

    message:
        label: opiy.faq::lang.models.message.columns.message
        searchable: true
        type: text_preview
        width: 50%

    remote_ip:
        label: opiy.faq::lang.models.message.columns.remote_ip
        searchable: true
        type: text
        invisible: true
